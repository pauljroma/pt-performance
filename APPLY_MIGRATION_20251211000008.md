# Apply Migration 20251211000008 - Fix Program Viewer Schema

## Problem
Build 12 shows "data missing" for programs because:
1. iOS queries `sessions.session_number` but table has `sequence`
2. iOS queries `exercise_templates.exercise_name` but table has `name`

## Solution
Apply migration that adds alias columns with automatic sync triggers.

## Instructions

### Option 1: Supabase Dashboard (FASTEST - 2 minutes)

1. **Open Supabase SQL Editor:**
   ```
   https://supabase.com/dashboard/project/rpbxeaxlaoyoqkohytlw/sql/new
   ```

2. **Copy the entire contents of:**
   ```
   clients/linear-bootstrap/supabase/migrations/20251211000008_fix_program_viewer_schema.sql
   ```

3. **Paste into SQL Editor and click "Run"**

4. **Verify success** - You should see:
   ```
   ✅ PROGRAM VIEWER SCHEMA FIXED
   ✅ Program viewer should now work!
   ```

5. **Test immediately:**
   ```bash
   cd clients/linear-bootstrap
   python3 test_program_viewer_fixed.py
   ```

### Option 2: Command Line (if you have Supabase CLI configured)

```bash
cd clients/linear-bootstrap/supabase
supabase db push
```

## Verification

After applying, run the test to confirm it works:

```bash
python3 test_program_viewer_fixed.py
```

Expected output:
- ✅ sessions.session_number column exists
- ✅ exercise_templates.exercise_name column exists
- ✅ Program query returns all data
- ✅ Sessions query works
- ✅ Exercises query works

## What This Migration Does

1. **Adds `sessions.session_number`** column (synced with `sequence`)
2. **Adds `exercise_templates.exercise_name`** column (synced with `name`)
3. **Creates triggers** to keep columns in sync automatically
4. **Syncs existing data** for all rows

## Impact

- ✅ Program viewer will load successfully
- ✅ No iOS code changes needed
- ✅ No breaking changes (both column names work)
