# Root Cleanup Archive (2026-02-05)

This folder contains historical status/report artifacts moved out of the repository root to improve discoverability of active code and primary documentation.

Notes:
- Files were moved with `git mv` to preserve history.
- No runtime code paths were changed.
- If any of these docs should be promoted back into `/docs`, they can be moved in a follow-up PR.
