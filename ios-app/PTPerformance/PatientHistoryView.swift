import SwiftUI

struct PatientHistoryView: View {
    var body: some View {
        NavigationView {
            List {
                Text("Recent sessions placeholder")
                Text("Pain trend chart placeholder")
            }
            .navigationTitle("History")
        }
    }
}
