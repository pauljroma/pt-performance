# __NAME__ (Swift Package)

Generated by tool-chain `generate ios_lib __NAME__`.

## Requirements

- Swift 5.10+
- Xcode 16+ or Swift toolchain

## Build

```bash
swift build --package-path packages/__NAME__
# or
npx nx run __NAME__:build
```

## Test

```bash
swift test --package-path packages/__NAME__
# or
npx nx run __NAME__:test:unit
```

## Integration

Add to your Xcode project via SPM:
```
File → Add Package Dependencies → local path: packages/__NAME__
```
