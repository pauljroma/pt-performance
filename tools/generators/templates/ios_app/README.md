# __NAME__ (iOS App)

Generated by tool-chain `generate ios_app __NAME__`.

## Requirements

- Xcode 16+
- iOS 17+ deployment target
- Swift 5.10+

## Build

```bash
# Local simulator build
./ios-app/build_local.sh

# CI (via Nx)
npx nx run __NAME__:build
```

## Lint

```bash
swiftlint
# or
npx nx run __NAME__:lint
```

## Test

```bash
npx nx run __NAME__:test:unit
```

## Architecture

- MVVM + SwiftUI
- `@MainActor` on all ViewModels
- `async/await` for network
- See CLAUDE.md for full guidelines
